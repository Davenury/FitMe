import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    baseText: {
       fontFamily: 'Roboto',
       fontSize: 16,
       color: '#ffffff',
    },
    titleText: {
      color: '#fff',
      fontSize: 24,
      fontWeight: "bold"
    }
  });